//calcula o tamanho da casinha de cachorro  que possa ser adquirida
//dado o orçamento de usuário
#include <iostream>
#include <cmath>
#include <cstdlib>

/*int main()
{
	const double COST_PER_SQ_FT = 10.50;
	double orcamento, area, comprimentoLado;
	
	std::cout << "Informe quanto quer gastar com a casinha de cachorro (em R$)?" << std::endl;
	std::cin >> orcamento;
	
	area = orcamento/COST_PER_SQ_FT;
	comprimentoLado = sqrt(area);
	
	std::cout.setf(std::ios::fixed);
	std::cout.setf(std::ios::showpoint);
	std::cout.precision(2);
	std::cout << "Por um preco de $ " << orcamento << std::endl
	          << "Posso lhe construir uma magnifica casinha de cachorro " << std::endl
			  << "com: " << comprimentoLado << " pes em cada lado. \n" << std::endl;
			  
	exit(1);
	//return 0;	
}*/

